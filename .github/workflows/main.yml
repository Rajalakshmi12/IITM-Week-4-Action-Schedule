name: Master
on:
  push:
    branches:
      - master
  schedule:
    - cron: '48 21 * * 6'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Running
      run: |
        echo "Running!"
